# yaml-language-server: $schema=https://raw.githubusercontent.com/csm-actions/securefix-action/main/json-schema/config.json
entries:
  - client:
      repositories:
        - csm-actions/test-client
      branches:
        - main
        - test-209
        - feat-root-dir
    push:
      repositories:
        - csm-actions/test-client
      branches:
        - "test-*" # Glob
        - gh-pages
    # Allow to create pull requests
    pull_request:
      base_branches:
        - main
